# -*- mode: snippet -*-
# name: arr.map(function(item, i, arr) {...})
# key: mapfu
# --
`(unless (eq (char-before) ?.) ".")`map(function(${1:item}, ${2:i}, ${3:arr}) {
    `(!%!)`$0
})`(if (eolp) ";")`