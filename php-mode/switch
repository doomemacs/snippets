# -*- mode: snippet -*-
# name: switch (...) {...}
# key: switch
# uuid: switch
# --
switch (\$${1:expression}) {
    case ${2:condition}:
        ${3:statement}
        break;
    default:
        $0
}