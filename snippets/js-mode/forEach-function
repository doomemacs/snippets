# -*- mode: snippet -*-
# name: arr.forEach(function(item) {...})
# key: forefu
# --
`(unless (eq (char-before) ?.) ".")`forEach(function(${1:item}) {
    `(doom-snippets-format "%!%s%!")`$0
})`(if (eolp) ";")`